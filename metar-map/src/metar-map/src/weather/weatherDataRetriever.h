#import "stationsToLed.h"

String buildWeatherRetrievingUrl(String baseUrl, WeatherStation metarStarions[]) 
{
    uint8_t numberOfStations = (uint8_t)(sizeof(metarStarions) / sizeof(WeatherStation));

    return "";
}